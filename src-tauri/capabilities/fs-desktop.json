{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "fs-desktop",
  "description": "File system access for desktop platforms (scoped to app data directory)",
  "windows": ["main"],
  "permissions": [
    "core:path:default"
  ]
}

